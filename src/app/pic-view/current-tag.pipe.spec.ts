import { CurrentTagPipe } from './current-tag.pipe';

describe('CurrentTagPipe', () => {
  it('create an instance', () => {
    const pipe = new CurrentTagPipe();
    expect(pipe).toBeTruthy();
  });
});
